package ex_P001;

public class exerc_teorico {
/*
1. 
Uma classe em Java  é um modelo a partir do qual os objetos são criados. Portanto, um objeto é uma instância de uma classe. A classe define os estados e comportamentos que um objeto pode ter, enquanto um objeto é uma entidade que possui estados e comportamentos definidos pela sua classe.
	Exemplos:
		*Java:
			class Carro {
			    String cor;
			    String modelo;
			}
			Carro meuCarro = new Carro();
			
		
		*C++:
			class Carro {
			    public:
			        string cor;
			        string modelo;
			};
			Carro meuCarro;
2.
Em Java, uma variável é declarada especificando o tipo de dados seguido pelo nome da variável. Os tipos de dados primitivos mais comuns em Java são int, char, double, boolean, etc. 
Em C++, a declaração de variáveis é semelhante à de Java. Os tipos de dados primitivos mais comuns em C++ são int, char, double, bool, etc.

3.
A herança é um mecanismo em Java que permite que uma classe adquira as propriedades e comportamentos de outra classe. A classe que é herdada é chamada de classe base ou superclasse, e a classe que herda é chamada de classe derivada ou subclasse.Em Java, você pode criar uma subclasse a partir de uma classe existente usando a palavra-chave extends.

4.
Em Java, quando declaramos uma variável de um tipo de objeto, na verdade temos uma referência (semelhante a um ponteiro) para o objeto na memória heap. No entanto, ao contrário dos ponteiros em C++, as referências em Java são abstraídas e não podemos manipulá-las diretamente.
Em C++, um ponteiro é uma variável que contém o endereço de memória de outra variável. Os ponteiros em C++ podem ser manipulados diretamente, o que permite uma maior flexibilidade, mas também pode levar a erros se não for manuseado corretamente.

 */
}
